name: format-test-probe

on:
  pull_request:

permissions:
  contents: read

jobs:
  format:
    if: github.event.pull_request.number == 6
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.21.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Print formatted test
        shell: bash
        run: |
          pnpm exec prettier --write apps/web/src/data/bible/github-release-download.test.ts
          base64 -w0 apps/web/src/data/bible/github-release-download.test.ts
          echo
